[package]
name = "flexui-resource"
version = "0.1.0"
edition = "2021"

[dependencies]
# 只保留纯 Rust 的压缩(deflate/flate2)与 AES，去掉 bzip2/zstd/lzma 等 C 依赖以便交叉编译。
zip = { version = "2", default-features = false, features = ["aes-crypto", "deflate"] }
