[package]
name = "flexui-examples"
version = "0.1.0"
edition = "2021"

[dependencies]
flexui = { path = "../flexui" }

[build-dependencies]
winresource = { version = "0.1.31", default-features = false }
